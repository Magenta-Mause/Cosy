apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cosy-frontend
spec:
  rules:
    - host: DOMAIN_PLACEHOLDER
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cosy-frontend
                port:
                  number: 80
