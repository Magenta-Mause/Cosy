apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cosy-backend
spec:
  tls:
    - hosts:
        - DOMAIN_PLACEHOLDER
      secretName: cosy-tls
  rules:
    - host: DOMAIN_PLACEHOLDER
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: cosy-backend
                port:
                  number: 80
